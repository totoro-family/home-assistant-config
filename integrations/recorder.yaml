# https://www.home-assistant.io/integrations/recorder/
recorder:
  db_url: !secret recorder_db_url
  purge_keep_days: 420
  exclude:
    domains:
      - camera
    entities:
      - sun.sun
      - sensor.last_boot
      - sensor.date
      - sensor.time
