type: custom:fold-entity-row
head:
  entity: binary_sensor.bedroom_windows
  tap_action: { action: fire-dom-event, fold_row: true }
entities:
  - !include window-left.yaml
  - !include window-right.yaml
